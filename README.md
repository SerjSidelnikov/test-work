# Тестовое задание для компании ЭТМ

### `npm run server`

Запускает сервер для получения данных
на [http://localhost:3001](http://localhost:3001).

### `npm start`

Запускает приложение
на [http://localhost:3000](http://localhost:3000).

